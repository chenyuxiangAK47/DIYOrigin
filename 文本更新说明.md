# 文本更新说明

## 已完成的工作

### 1. 创建详细配置文件
- **文件位置**: `OriginSystemMod/ModuleData/OriginsMenu_Khuzait_Detailed.txt`
- **内容**: 包含所有10个预设出身的详细配置，每个选项都有：
  - 显示文本（text）
  - 效果说明（effects），包括：
    - 金币/食物/马匹变化
    - 声望/士气变化
    - 关系变化
    - 技能加成
    - 标记（flags）
    - 初始兵力
    - 装备包

### 2. 代码文本更新（部分完成）

已更新的预设出身：
- ✅ **草原叛酋** (rebel_chief) - 全部4个节点已更新
- ✅ **迁徙王族** (wandering_prince) - Node1 已更新
- ✅ **汗廷旁支贵族** (minor_noble) - Node1 已更新

## 待更新的预设出身

以下预设出身的文本需要按照详细配置文件更新：

1. **西迁部族首领** (migrant_chief) - 4个节点
2. **汗廷军叛逃者** (army_deserter) - 4个节点
3. **草原商路守护者** (trade_protector) - 4个节点
4. **可汗的雇佣战帮** (khans_mercenary) - 4个节点
5. **逃出沙漠的战奴** (slave_escape) - 5个节点
6. **草原自由民** (free_cossack) - 4个节点
7. **东方旧部复仇者** (old_guard_avenger) - 4个节点
8. **迁徙王族** (wandering_prince) - Node2-4 待更新
9. **汗廷旁支贵族** (minor_noble) - Node2-4 待更新

## 更新模式

每个选项的更新模式：

**旧格式**:
```csharp
new NarrativeMenuOption("option_id", new TextObject("简短标题"), new TextObject("简短描述.效果1,效果2"), ...)
```

**新格式**:
```csharp
new NarrativeMenuOption("option_id", new TextObject("详细标题"), new TextObject("详细描述。效果1，效果2，标记：xxx"), ...)
```

## 更新示例

### 示例1：草原叛酋 Node1
**旧**:
```csharp
new NarrativeMenuOption("rebel_reason_refuse_tribute", 
    new TextObject("拒绝纳贡"), 
    new TextObject("你拒绝向汗廷纳贡.与可汗宫廷关系--,草原通缉+"), ...)
```

**新**:
```csharp
new NarrativeMenuOption("rebel_reason_refuse_tribute", 
    new TextObject("拒绝纳贡"), 
    new TextObject("你拒绝向汗廷纳贡。声望+1，与库塞特关系-5，标记：被怀疑"), ...)
```

### 示例2：草原叛酋 Node2
**旧**:
```csharp
new NarrativeMenuOption("rebel_warband_nomads", 
    new TextObject("游牧战士"), 
    new TextObject("你的战帮是游牧战士.初始兵力:游牧战士×20"), ...)
```

**新**:
```csharp
new NarrativeMenuOption("rebel_warband_nomads", 
    new TextObject("以游牧战士为主"), 
    new TextObject("你的战帮以游牧战士为主。初始兵力：游牧战士×16，部落青年×6，食物+8"), ...)
```

## 后续工作

1. **继续更新代码文本**：按照详细配置文件逐个更新剩余选项
2. **实现效果系统**：根据 effects 配置实现实际的效果应用（金币、技能、关系等）
3. **测试验证**：确保所有文本显示正确，效果应用正确

## 注意事项

- 所有文本都已保存在 `OriginsMenu_Khuzait_Detailed.txt` 中
- 更新代码时请参考该配置文件
- 保持代码格式一致，注意中英文标点符号
- 效果数值可以根据游戏平衡性调整








































